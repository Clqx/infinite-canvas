services:
  app:
    image: infinite-canvas:local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    environment:
      AUTH_MODE: disabled
      AUTH_ALLOW_INSECURE_LOOPBACK: "1"
